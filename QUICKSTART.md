# 🚀 Быстрый старт

## Установка и запуск за 5 минут

### 1. Подготовка

```bash
# Убедитесь, что Python 3.8+ установлен
python3 --version

# Убедитесь, что ffmpeg установлен (macOS)
brew install ffmpeg
```

### 2. Установка зависимостей

```bash
# Активируйте виртуальное окружение (уже создано)
source venv/bin/activate  # macOS/Linux
# или
venv\Scripts\activate     # Windows

# Установите зависимости
pip install -r requirements.txt
```

⏱️ **Время**: ~10-15 минут (зависит от скорости интернета)

### 3. Первый запуск

```bash
# Простой способ (macOS/Linux)
chmod +x run.sh
./run.sh

# Или напрямую
python main.py

# Windows
run.bat
```

При первом запуске приложение скачает необходимые модели (~7-8 GB). Это займет 15-30 минут.

### 4. Пробный запуск

1. Откроется окно приложения
2. Нажмите **"Load File"** и выберите аудио/видео файл
3. (Опционально) Нажмите **"Load Reference"** и выберите образец вашего голоса
4. Выберите режим: **"Быстрый"** для первого теста
5. Нажмите **"Обработать"**
6. Дождитесь завершения
7. Нажмите **"Открыть папку с результатами"**

## Минимальный пример использования

### Для тестирования без референса голоса:

1. Запустите приложение: `python main.py`
2. Загрузите любой аудио файл с русской речью
3. Оставьте настройки по умолчанию
4. Нажмите "Обработать"
5. Результат появится в папке `output/`

### С вашим голосом:

1. Запишите образец своего голоса (10-20 секунд, WAV формат)
2. Загрузите его как "Voice Reference"
3. Обработайте ваш файл
4. TTS будет использовать ваш голос для английской речи

## Что делать если что-то не работает?

### Ошибка при импорте модулей
```bash
pip install --upgrade -r requirements.txt
```

### ffmpeg не найден
```bash
# macOS
brew install ffmpeg

# Ubuntu/Debian
sudo apt-get install ffmpeg

# Windows - скачайте с ffmpeg.org и добавьте в PATH
```

### Недостаточно памяти
Отредактируйте `config.py`:
```python
ASR_CONFIG = {
    "model_name": "medium",  # вместо large-v3
    "compute_type": "int8",   # вместо float16
}
```

### Долго обрабатывается
- Это нормально для первого запуска
- Для 1 минуты аудио ожидайте ~2-5 минут обработки на CPU
- С GPU будет в 3-4 раза быстрее

## Структура результатов

После обработки в папке `output/` появятся:

```
output/
├── yourfile_transcription.txt      # Распознанный русский текст
├── yourfile_russian.srt            # Русские субтитры
├── yourfile_translation.txt        # Перевод с таймкодами
├── yourfile_translated_audio.wav   # Переведенное аудио
└── yourfile_translated.mp4         # Видео с новой дорожкой (если вход был видео)
```

## Следующие шаги

1. Ознакомьтесь с полным [README.md](README.md)
2. Настройте параметры в [config.py](config.py)
3. Экспериментируйте с разными режимами обработки
4. Попробуйте записать качественный референс голоса для лучших результатов

## Поддержка

Если возникли проблемы:
1. Проверьте раздел "Устранение проблем" в README.md
2. Посмотрите логи в папке `logs/`
3. Создайте issue с описанием проблемы

---

**Совет**: Для первого теста используйте короткий файл (30-60 секунд) и режим "Быстрый". Это позволит быстро увидеть результат и понять, как работает система.
